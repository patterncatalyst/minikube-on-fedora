apiVersion: v2
name: inventory-service
description: inventory-service data product — FastAPI service backed by the capstone Postgres (inventory schema). r22 ships the health skeleton; domain surface added later.
type: application
version: 0.1.0
appVersion: "0.1.0"
