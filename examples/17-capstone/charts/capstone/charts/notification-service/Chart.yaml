apiVersion: v2
name: notification-service
description: notification-service data product — FastAPI service backed by the capstone Postgres (notifications schema). r22 ships the health skeleton; domain surface added later.
type: application
version: 0.1.0
appVersion: "0.1.0"
