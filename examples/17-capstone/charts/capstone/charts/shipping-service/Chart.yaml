apiVersion: v2
name: shipping-service
description: shipping-service data product — FastAPI service backed by the capstone Postgres (shipping schema). r22 ships the health skeleton; domain surface added later.
type: application
version: 0.1.0
appVersion: "0.1.0"
